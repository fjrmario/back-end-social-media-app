var express = require('express');
var router = express.Router();
const profilesController = require("../controllers/profiles")


// router.get('/:id', profilesController.showProfilePublicly )

module.exports = router